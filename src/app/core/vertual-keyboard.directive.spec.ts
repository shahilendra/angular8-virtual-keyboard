import { VertualKeyboardDirective } from './vertual-keyboard.directive';

describe('VertualKeyboardDirective', () => {
  it('should create an instance', () => {
    const directive = new VertualKeyboardDirective();
    expect(directive).toBeTruthy();
  });
});
